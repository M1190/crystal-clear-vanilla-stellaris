namespace = op_game_start
### Empire init - add technologies and resources
country_event = {	
	id = op_game_start.1
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		if = { 																	
			limit = { 
				has_civic = civic_legendary_fleet
			} 
			give_technology = { message = no tech = tech_destroyers }		# Gives empire Destroyer technology at game start, disabled for now due to NSC clashes
		}
			}
}

country_event = {	
	id = op_game_start.2
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		if = { 																	
			limit = { 
				has_civic = civic_wondrous_vault
			} 
			give_technology = { message = no tech = tech_mine_volatile_motes }		
		}
			}
}

country_event = {	
	id = op_game_start.3
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		if = { 																	
			limit = { 
				has_civic = civic_wondrous_vault
			} 
			give_technology = { message = no tech = tech_mine_exotic_gases }		
		}
			}
}

country_event = {	
	id = op_game_start.4
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		if = { 																	
			limit = { 
				has_civic = civic_wondrous_vault
			} 
			give_technology = { message = no tech = tech_mine_rare_crystals }		
		}
			}
}

country_event = {	
	id = op_game_start.5
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		if = { 																	
			limit = { 
				has_civic = civic_psionic_destiny
			} 
			give_technology = { message = no tech = tech_psionic_theory }
			give_technology = { message = no tech = tech_mine_zro }		
		}
			}
}

country_event = {
	id = op_game_start.6
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		if = { 																	
			limit = { 
				has_civic = civic_abhorrent_ascendancy
			} 
			#give_technology = { message = no tech = tech_psionic_theory }		
			capital_scope = {
				planet = {
					add_building = building_high_arcology
					add_building = building_low_arcology
					#remove_building = building_research_lab_1
				}
			}
		}
			}
}

country_event = {	
	id = op_game_start.7
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		if = { 																	
			limit = { 
				has_civic = civic_legendary_fleet
			} 
			give_technology = { message = no tech = tech_mine_volatile_motes }
			give_technology = { message = no tech = tech_mine_exotic_gases }
			give_technology = { message = no tech = tech_mine_rare_crystals }
		}
			}
}